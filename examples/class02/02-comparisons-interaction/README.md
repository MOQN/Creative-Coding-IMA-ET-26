# Comparisons & Interaction

## if statements

Change the diameter of the circle based on the `mouseX` position.

<div id="code-01"></div>
<br>

Change the color of the circle based on the `mouseY` position.
<div id="code-02"></div>
<br>

Draw circles when the mouse is pressed. (`mouseIsPressed` is `true`.)
<div id="code-03"></div>

Draw background when **spacebar** is pressed. (`keyIsPressed` is `true` and `key` is `" "`.)
<div id="code-04"></div>


<script>
  const numberOfCodeBlocks = 4;
  const folderName = 'examples/class02/02-comparisons-interaction/';

  loadCodeBlocks(folderName, numberOfCodeBlocks);
</script>