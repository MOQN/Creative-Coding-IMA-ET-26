function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(0); // black


  //noStroke(); // no outline
  noFill(); // no fill
  stroke(255); // white
  circle(width / 2, height / 2, 250);
}