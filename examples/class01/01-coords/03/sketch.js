function setup() {
  // runs once, at the beginning
  createCanvas(400, 400);

  background(220);
  textSize(32);
  text(frameCount, 50, 100); // (txt, x, y)
}

function draw() {
  // loops continuously
}